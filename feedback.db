import sqlite3

conn = sqlite3.connect("feedback.db")
conn.execute("""
CREATE TABLE IF NOT EXISTS feedback(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    course TEXT,
    rating INTEGER,
    message TEXT,
    date TEXT
)
""")
conn.commit()
conn.close()

print("feedback.db created successfully!")
